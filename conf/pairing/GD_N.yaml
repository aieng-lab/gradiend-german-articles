plot_name: 'GD_N'
palette:  {'G': 'purple','D': 'orange'}
categories:
  G:
    labels: [GN] # excluding GF as it uses the same definite singular article 'der' as DF
  D:
    labels: [DN] # excluding DF as it uses the same definite singular article 'der' as GF
